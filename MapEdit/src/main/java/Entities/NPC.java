package Entities;

import Data.ImageObject;

import java.awt.*;

public class NPC extends Entity {

    public NPC(Point position, String name, Point size, ImageObject imageObject) {
        super(position, name, size, imageObject);
    }

    @Override
    public void render() {
        super.render();
    }
}
